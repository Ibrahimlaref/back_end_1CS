# Rollback Manifest: users 0006_merge_20260310_0153

- Forward migration: `users.0006_merge_20260310_0153`
- Rollback target: `users.0005_user_retention_fields`

## Command
```bash
python manage.py migrate users 0005_user_retention_fields
```

## Notes
- This migration has no schema operations; rollback only reopens the migration graph to the retention branch head.
- Use this only if you intentionally need to back out the merge node while keeping the retention fields applied.

## Verification
```sql
SELECT 1
FROM django_migrations
WHERE app = 'users' AND name = '0006_merge_20260310_0153';
```

- Expect no rows after rollback.
